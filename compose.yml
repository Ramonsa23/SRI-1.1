services:
  darthvader:
    build: dnsserver
    hostname: darthvader
    networks:
      rededns:
          ipv4_address: 192.168.20.10
    volumes:
      - ./dnsserver/conf/named.conf.options:/etc/bind/named.conf.options
      - ./dnsserver/conf/db.starwars:/etc/bind/db.starwars
      - ./dnsserver/conf/named.conf.local:/etc/bind/named.conf.local
  eq1:
    build: debian
    hostname: eq1
    dns:
      - 192.168.20.10
    networks:
      rededns:
          ipv4_address: 192.168.20.100
networks:
  rededns:
    ipam:
      config:
        - subnet: 192.168.20.0/24

